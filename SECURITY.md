# 安全说明

## 🔒 敏感信息处理

本项目已对以下敏感信息进行了安全处理：

### 已清理的敏感信息

1. **飞书 API 凭证**
   - `FEISHU_APP_ID`: 已替换为环境变量 `{{ $env.FEISHU_APP_ID }}`
   - `FEISHU_APP_SECRET`: 已替换为环境变量 `{{ $env.FEISHU_APP_SECRET }}`
   - `FEISHU_TABLE_APP_TOKEN`: 已替换为环境变量 `{{ $env.FEISHU_TABLE_APP_TOKEN }}`
   - `FEISHU_TABLE_ID`: 已替换为环境变量 `{{ $env.FEISHU_TABLE_ID }}`
   - `FEISHU_WEBHOOK_URL`: 已替换为环境变量 `{{ $env.FEISHU_WEBHOOK_URL }}`

2. **DeepSeek API 凭证**
   - DeepSeek API 凭证ID: 已替换为环境变量 `{{ $env.DEEPSEEK_API_CREDENTIAL_ID }}`

3. **微博 API 凭证**
   - `WEIBO_APP_KEY`: 已替换为环境变量 `{{ $env.WEIBO_APP_KEY }}`
   - `WEIBO_APP_SECRET`: 已替换为环境变量 `{{ $env.WEIBO_APP_SECRET }}`
   - `WEIBO_ACCESS_TOKEN`: 已替换为环境变量 `{{ $env.WEIBO_ACCESS_TOKEN }}`
   - `WEIBO_UID`: 已替换为环境变量 `{{ $env.WEIBO_UID }}`
   - `WEIBO_AUTH_CODE`: 已替换为环境变量 `{{ $env.WEIBO_AUTH_CODE }}`

## 🛡️ 安全最佳实践

### 1. 环境变量管理

- ✅ 使用 `.env` 文件存储敏感配置
- ✅ 将 `.env` 文件添加到 `.gitignore`
- ✅ 提供 `env.template` 作为配置模板
- ✅ 在文档中说明如何获取各种 API 凭证

### 2. 凭证安全

- 🔐 **定期轮换**: 建议每 3-6 个月轮换一次 API 密钥
- 🔐 **权限最小化**: 只授予应用必要的最小权限
- 🔐 **监控使用**: 定期检查 API 使用情况和异常调用
- 🔐 **安全存储**: 使用安全的密码管理器存储敏感信息

### 3. 网络安全

- 🌐 **HTTPS**: 所有 API 调用都使用 HTTPS
- 🌐 **访问控制**: 限制 API 访问的 IP 范围（如果支持）
- 🌐 **速率限制**: 配置合理的 API 调用频率限制

### 4. 数据安全

- 📊 **数据加密**: 敏感数据在传输和存储时进行加密
- 📊 **访问日志**: 记录和监控数据访问日志
- 📊 **数据备份**: 定期备份重要数据
- 📊 **数据清理**: 定期清理过期或不需要的数据

## ⚠️ 安全警告

### 使用前必须完成的安全配置

1. **创建 `.env` 文件**
   ```bash
   cp env.template .env
   # 编辑 .env 文件，填入真实的 API 凭证
   ```

2. **验证凭证有效性**
   - 测试所有 API 连接
   - 确认权限设置正确
   - 验证数据访问正常

3. **配置访问控制**
   - 限制 n8n 的访问权限
   - 配置防火墙规则
   - 启用访问日志

### 常见安全风险

1. **凭证泄露**
   - ❌ 不要在代码中硬编码 API 密钥
   - ❌ 不要将 `.env` 文件提交到版本控制
   - ❌ 不要在公开场所展示 API 密钥

2. **权限过度**
   - ❌ 不要授予不必要的 API 权限
   - ❌ 不要使用管理员权限运行应用
   - ❌ 不要共享 API 凭证

3. **网络安全**
   - ❌ 不要在不安全的网络环境中运行
   - ❌ 不要使用 HTTP 传输敏感数据
   - ❌ 不要忽略 SSL 证书警告

## 🔍 安全检查清单

部署前请确认：

- [ ] 所有敏感信息已移至环境变量
- [ ] `.env` 文件已添加到 `.gitignore`
- [ ] API 凭证已正确配置并测试
- [ ] 网络连接使用 HTTPS
- [ ] 访问权限已最小化
- [ ] 监控和日志已配置
- [ ] 数据备份策略已制定
- [ ] 安全更新计划已制定

## 🚨 安全事件响应

如果发现安全事件：

1. **立即行动**
   - 立即撤销泄露的 API 密钥
   - 停止相关服务
   - 检查系统完整性

2. **调查分析**
   - 分析事件原因和影响范围
   - 收集相关日志和证据
   - 评估数据泄露情况

3. **恢复措施**
   - 更新所有相关凭证
   - 修复安全漏洞
   - 加强安全监控

4. **预防措施**
   - 更新安全策略
   - 加强安全培训
   - 定期安全审计



---

**重要提醒**: 本项目的所有敏感信息已从代码中移除，但使用前必须正确配置环境变量。请务必遵循安全最佳实践，保护你的 API 凭证和数据安全。
